"""SCRATCH -- deliberate failure proving ledger/checks is really collected."""


def test_ledger_root_red_proof_deliberate_failure():
    assert 1 == 2, "deliberate CI red-proof failure (ledger/checks root)"
